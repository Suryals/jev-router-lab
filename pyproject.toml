[project]
name = "jev-router-lab"
version = "0.1.0"
description = "Shadow-mode evaluation of TypeSafe Jev as an AIOps triage router"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.7.0",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
